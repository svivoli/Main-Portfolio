import React from 'react';
import '../style.css';

function ErrorMessage() {
    return(
        <div className="error">
            <h3 className="title">Page does not exist</h3>
        </div>
    )
};

export default ErrorMessage;